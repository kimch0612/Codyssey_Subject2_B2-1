# Study Note — B2-1 (콘솔 가계부)

과제(Subject.txt)를 구현하기 전에 알아둬야 할 개념·용어와 진행 계획 정리.
보너스 과제(5장)는 기본 요구사항(4장)을 모두 끝낸 뒤에 검토한다.

## 이 과제의 학습 목적

- 데이터를 "저장하는 것"과 "안전하게 저장하는 것"의 차이를 코드로 체감하기
- 파일 하나에 몰아넣지 않고 **계층(모델/저장소/서비스/CLI)으로 책임을 나누는 감각** 익히기
- 제너레이터·데코레이터·타입 힌트를 "문법 연습"이 아니라 **문제를 풀기 위해** 쓰기

과제를 마친 뒤 스스로 설명할 수 있어야 하는 것:
- 파일 기반 저장(JSONL/CSV)으로 CRUD·검색·요약·입출력을 구현하는 방법
- 각 계층(모델/저장소/서비스/CLI)이 **무엇을 알고, 무엇을 몰라야 하는가**
- `yield` 기반 제너레이터로 스트리밍 처리하는 이유와 동작 방식
- 데코레이터로 공통 관심사(로그/예외/시간)를 분리한 구조와 그 이유
- 타입 힌트로 입출력 계약을 명확히 했을 때 얻는 이점 (실제 코드 예시로)

## 최종 산출물

| 산출물 | 최소 규격 |
|---|---|
| 애플리케이션 | `add / list / search / summary / budget / category / update / delete / import / export` **10개 명령이 정상 동작** |
| 데이터 파일 | `data/` 아래 **3개 이상** 영구 저장 (`transactions` / `categories` / `budgets`) |
| 모듈 구조 | **3개 이상 모듈**로 분리, **클래스 2개 이상** |
| GitHub 리포지토리 | 소스 push, 기능 단위 커밋 히스토리 확인 가능 |
| README.md | 실행 방법 / 저장 파일 위치·형식 / 주요 명령 예시 / **import·export CSV 스키마** 포함 |

## 핵심 제약 조건

- **표준 라이브러리만** 사용 — `pip install`이 필요한 외부 패키지 전면 금지
- **Python 3.10 이상**
- 저장 포맷은 JSONL 또는 CSV 중 **1개 선택**, 저장 파일은 **3개 이상**으로 분리
- 옵션 표기는 **`-`(단일 대시)로 통일** (아래 주의사항 참고)
- 오류 시 **스택트레이스 출력 금지** — 원인 + 해결 힌트를 사람이 읽을 문장으로 출력
- 정상 종료 `exit code 0`, 오류 종료는 **0이 아닌 값**
- 모든 명령이 `-help`로 사용법을 출력해야 함
- 입력 기본 방식은 **대화형**(`add`는 반드시 대화형).
  `search / list / summary / export / import / delete`는 옵션 방식 허용(권장)
- 데코레이터를 **1개 이상 구현하고 실제로 적용**

### ⚠️ 옵션 표기 — 명세 안에서 4장과 7장이 충돌한다

명세가 두 군데서 다른 말을 한다.

> **7장 제약 사항** — "옵션 표기는 `-`로 통일"
> **4장 기능 요구사항** — "옵션 표기는 리눅스 표준인 `--`로 통일해야 합니다"

원본 과제 페이지를 확인한 결과 **7장의 `-`는 복붙 사고가 아니라 실제 사양**이다.
7장은 "제약 사항"이므로 이쪽을 따른다. → **모든 옵션은 `-limit`, `-from`, `-data-dir` 형태의 단일 대시.**

`-`가 리눅스 관례(단일 대시는 단일 문자, 여러 글자는 이중 대시)와 어긋나는 건 사실이지만,
과제 채점 기준이 명세이므로 관례보다 명세를 우선한다.
argparse는 `parser.add_argument("-limit", type=int)` 형태로 단일 대시 다중 문자 옵션을 **문제없이 지원**한다.

#### 단, `help`만은 예외 처리가 필요하다

- argparse는 기본적으로 `-h` / `--help`를 **자동으로** 붙인다. `-help`는 자동으로 안 생긴다.
- 반면 4장은 "모든 명령은 `--help` 옵션으로 사용 방법이 출력되어야 합니다"라고 `--help`를 콕 집어 요구한다.

두 요구를 동시에 만족시키는 방법 — **둘 다 등록한다**:

```python
parser = argparse.ArgumentParser(add_help=False)
parser.add_argument("-help", "--help", action="help",
                    help="사용 방법을 출력하고 종료합니다")
```

`add_help=False`로 자동 생성을 끄고 직접 두 표기를 모두 붙이면 `-help`와 `--help`가 전부 동작한다.
서브커맨드에도 전부 적용해야 하므로 **부모 파서(`parents=[...]`)로 한 번만 정의**하는 게 맞다.

#### 관련 함정 — `from`은 파이썬 예약어다

`-from` 옵션은 `args.from`으로 접근하는데 **이건 문법 오류**다 (`from`이 예약어이므로).
반드시 `dest`를 따로 지정해야 한다:

```python
parser.add_argument("-from", dest="date_from")
parser.add_argument("-to",   dest="date_to")
```

`import` 명령의 `-from`, `export`의 `-from/-to`, `search`의 `-from/-to` 세 군데 모두 해당한다.
(`import` 자체도 예약어라 `def import(...)`은 불가 — 함수명은 `run_import` 등으로.)

---

## 확정된 설계 결정 (2026-09-03)

명세가 "둘 중 하나를 골라 문서에 명확히 고정하라"고 요구한 항목들. **README에도 그대로 옮겨 적을 것.**

| # | 항목 | 확정 | 이유 |
|---|---|---|---|
| ① | 저장 포맷 | **JSONL** | `tags`가 리스트, `amount`가 정수, `memo`가 선택 필드 — CSV로 누르면 타입이 전부 문자열로 뭉개진다. 또 내부 저장(JSONL)과 외부 교환(CSV)을 분리해야 직렬화 경계가 뚜렷해진다 |
| ② | 초기 카테고리 | **안 A / 파일이 없을 때만** `food, transport, rent, etc` 시드 | "비어있으면 시드"로 만들면 사용자가 전부 지운 뒤 재실행할 때 기본값이 부활한다. **"없을 때"와 "비었을 때"는 다르다** |
| ③ | `update` 입력 | **옵션 기반** | `update -id TX-12 -amount 20000`. 미지정 옵션을 `None`으로 두고 `dataclasses.replace()`로 부분 수정 — 타입 힌트·dataclass 학습과 직결되고 README 예시를 복붙 가능하게 쓸 수 있다 |
| ④ | 모듈 구조 | **계층형 4모듈** | `models / storage / services / cli`. 과제 목표가 "각 계층의 책임을 설명할 수 있다"이므로 분리 축을 계층으로 잡으면 목표와 1:1로 대응한다 |
| ⑤ | 최신순 정렬 | **`heapq.nlargest`** | 파일을 한 줄씩 끝까지 훑되 메모리는 `limit`개만 유지. "한 번에 로드하지 않는다"를 **메모리 기준**으로 충족하면서 날짜순 정확성도 보장 |
| ⑥ | id 발급 | **매 `add`마다 파일 스캔 후 max+1** | 카운터가 데이터에서 **파생**되므로 불일치가 구조적으로 불가능. 저장 파일도 3개로 유지 |
| ⑦ | 데코레이터 위치 | **CLI 진입점** | `print`와 `sys.exit`은 CLI의 책임이라는 계층 원칙과 일치. 서비스는 `raise`만 하고 예외 번역·종료 코드는 CLI 경계에서 |
| ⑧ | 데코레이터 도입 시점 | **11단계 (중복을 먼저 겪은 뒤 추출)** | 3~10단계에서 `try/except`를 반복해서 쓴 뒤 추출한다. 학습 목적상 의도된 순서 |

### ③의 세부 규칙 — `None`과 `""`의 구분

```
-amount 미지정   → None   → 기존 값 유지
-memo "점심"     → "점심"  → 교체
-memo ""         → ""     → 메모 삭제  (None이 아니다!)
```

`argparse`에서 `default=None`이면 "안 준 것"과 "빈 문자열로 준 것"이 자연히 구분된다. 이 규칙을 README에 한 줄로 명시하면 `update`의 애매함이 사라진다.

### 계층 경계 — "뭘 아는가"보다 "뭘 몰라야 하는가"

| 모듈 | 아는 것 | **몰라야 하는 것** |
|---|---|---|
| `models.py` | dataclass 필드, 타입, dict 변환 | 파일, `print`, 업무 규칙 |
| `storage.py` | 경로, JSON 한 줄, 원자적 교체 | "카테고리는 등록돼 있어야 한다" 같은 규칙 |
| `services.py` | 업무 규칙, 검증, 집계 | `argparse`, `input()`, `print()` |
| `cli.py` | 옵션 파싱, 프롬프트, 출력 포맷 | 파일 경로, JSON |

**자가진단 2문항** (구현 도중 수시로 확인):
1. 서비스 계층을 파이썬 REPL에서 `argparse` 없이 그대로 호출할 수 있는가?
2. 저장소 계층을 빈 임시 디렉토리에 대고, 업무 규칙 하나도 없이 테스트할 수 있는가?

하나라도 "아니오"면 계층이 샌 것이다.

---

## 용어 정리

실제 구현 순서(모델 → 저장소 → CLI → 기능 → 리팩터링)대로 정리.

### 1. 데이터 모델링

| 용어 | 뜻 | 왜 알아야 하나 |
|---|---|---|
| **`dataclass`** | `@dataclass` 데코레이터로 `__init__`/`__repr__`/`__eq__`를 자동 생성하는 표준 라이브러리 기능 | 명세가 "dataclass 또는 그에 준하는 구조"를 요구. `Transaction`을 이걸로 정의한다 |
| **타입 힌트** | `def f(x: int) -> str:` 처럼 인자·반환 타입을 코드에 명시하는 문법 | 실행 시 강제되지 않지만 **함수의 계약을 코드 자체가 설명**하게 만든다. 이 과제의 필수 요구사항 |
| **`Optional[str]` / `str \| None`** | "문자열이거나 없을 수 있음"을 뜻하는 타입 | `memo`가 선택 필드라 필요. 3.10+에선 `str \| None` 표기 가능 |
| **`list[str]`** | 문자열 리스트 타입 | `tags` 필드. 3.9+부터 `List` import 없이 소문자로 쓸 수 있다 |
| **`field(default_factory=list)`** | dataclass에서 리스트 같은 **가변 기본값**을 안전하게 주는 방법 | `tags: list[str] = []`로 쓰면 **모든 인스턴스가 같은 리스트를 공유**하는 고전적 버그가 난다. 반드시 `field(default_factory=list)` |
| **`dataclasses.asdict()`** | dataclass 인스턴스를 중첩까지 포함해 dict로 변환 | JSONL 저장 시 `json.dumps(asdict(tx))` 형태로 직렬화 |
| **`dataclasses.replace()`** | 기존 인스턴스에서 **일부 필드만 바꾼 새 인스턴스**를 만드는 함수 | `update`의 부분 수정을 한 줄로 구현. `replace(tx, amount=20000)` |
| **`frozen=True`** | dataclass를 불변(수정 불가)으로 만드는 옵션 | 불변으로 두면 "어디선가 몰래 값이 바뀌는" 버그가 원천 차단됨. `replace()`와 궁합이 좋다 — 쓸지 말지 판단해볼 것 |

### 2. 파일 기반 영구 저장

| 용어 | 뜻 | 왜 알아야 하나 |
|---|---|---|
| **JSONL (JSON Lines)** | 한 줄에 JSON 객체 하나씩 넣는 텍스트 포맷 (`.jsonl`) | **한 줄 = 한 레코드**라 append가 O(1)이고 줄 단위 스트리밍이 자연스럽다. 이 과제의 저장 포맷 |
| **`json.dumps(..., ensure_ascii=False)`** | 한글을 `\uXXXX`로 이스케이프하지 않고 그대로 저장하는 옵션 | 안 주면 `"메모": "점심"`처럼 저장돼 사람이 파일을 못 읽는다 |
| **append 모드 (`"a"`)** | 파일 끝에 이어 쓰는 열기 모드 | `add`는 전체 재작성 없이 한 줄만 덧붙이면 된다 |
| **원자적 교체 (atomic replace)** | 임시 파일에 전부 쓴 뒤 `os.replace()`로 원본과 바꿔치기하는 기법 | `update`/`delete`는 파일 전체 재작성이라, 쓰는 도중 죽으면 원본이 날아간다. `os.replace()`는 같은 파일시스템 안에서 **원자적**이라 중간 상태가 존재하지 않는다 |
| **`tempfile.NamedTemporaryFile`** | 임시 파일을 안전하게 만드는 표준 라이브러리 | 원자적 교체의 "임시 파일" 단계. **대상과 같은 디렉토리에** 만들어야 `os.replace()`가 원자성을 보장한다 |
| **`csv.DictReader` / `DictWriter`** | CSV를 dict 단위로 읽고 쓰는 표준 라이브러리 클래스 | `import`/`export` 전용. 따옴표·이스케이프를 알아서 처리해준다 |
| **`newline=""`** | CSV 파일을 열 때 반드시 줘야 하는 인자 | 안 주면 윈도우에서 **빈 줄이 한 줄씩 끼어든다**. csv 모듈 공식 문서가 명시하는 필수 사항 |
| **`pathlib.Path`** | 경로를 객체로 다루는 표준 라이브러리 | `Path(data_dir) / "transactions.jsonl"`, `.exists()`, `.mkdir(parents=True, exist_ok=True)` — 문자열 `os.path.join`보다 읽기 쉽다 |

### 3. 제너레이터 / 스트리밍

| 용어 | 뜻 | 왜 알아야 하나 |
|---|---|---|
| **제너레이터 (generator)** | `yield`를 쓴 함수. 호출해도 즉시 실행되지 않고, 값을 하나씩 요청받을 때마다 이어서 실행 | 명세가 `list`/`search`에 **명시적으로 요구**하는 구현 방식 |
| **`yield`** | 값을 하나 내보내고 **그 자리에서 함수 실행을 멈추는** 키워드 | `return`은 함수를 끝내지만 `yield`는 **일시정지**한다. 이 차이가 스트리밍의 전부 |
| **지연 평가 (lazy evaluation)** | 필요한 시점에야 값을 계산하는 방식 | 10만 줄짜리 파일에서 `-limit 3`이면 **3줄만 읽고 멈출 수 있다**. 리스트로 전부 읽으면 10만 줄을 다 메모리에 올린다 |
| **`Iterator` / `Iterable`** | `typing`의 제너레이터 반환 타입 힌트 | `def stream_transactions(...) -> Iterator[Transaction]:` — 반환값이 리스트가 아님을 계약으로 표현 |
| **`itertools.islice`** | 이터러블 앞에서 N개만 잘라내는 함수 | `-limit N` 구현. 리스트로 바꾸지 않고도 자를 수 있다 |
| **`heapq.nlargest`** | 전체를 정렬하지 않고 **상위 N개만** 뽑는 함수 (메모리 O(N)) | 아래 "스트리밍과 정렬의 충돌"을 푸는 한 가지 방법 |

#### 🔥 이 과제 최대의 설계 난제 — 스트리밍 vs 최신순 정렬

명세가 동시에 요구하는 두 가지:
- "파일 전체를 한 번에 로드하지 않고 **제너레이터 기반 스트리밍**으로 구현"
- "`list`는 **최신순**으로 출력"

그런데 **정렬은 원리적으로 전체를 봐야 한다.** 마지막 줄이 가장 오래된 날짜일 수도 있으니까.
검토한 세 가지 접근:

1. **파일을 입력순 append로 두고 역순으로 읽기** — 진짜로 N줄만 읽고 멈추는 가장 순수한 스트리밍. 그러나 "최신순"이 **날짜순이 아니라 입력순**이 되어, 과거 날짜를 나중에 입력하면 순서가 틀린다. `seek()`으로 줄 경계를 직접 찾는 구현도 까다롭다
2. **`heapq.nlargest(limit, stream, key=...)`** — 전체를 훑되 메모리는 `limit`개만 쓴다
3. 전부 리스트로 읽어서 `sorted()` — 가장 쉽지만 명세가 명시적으로 금지한 방식

### ✅ 결론 — 2번 (`heapq.nlargest`)

```python
import heapq
latest = heapq.nlargest(limit, stream_transactions(path),
                        key=lambda tx: (tx.date, tx.id))
```

**근거**: 명세의 "파일 전체를 한 번에 로드하지 않고"를 **메모리 기준**으로 해석하면 충족한다 —
파일이 10만 줄이든 메모리 사용량은 `limit`개로 고정된다. 동시에 날짜순 정확성도 보장된다.
1번은 정확성을 잃고, 3번은 명세를 정면으로 어긴다.

**정렬 키는 `(date, id)` 튜플**로 둔다. 같은 날짜가 여러 건일 때 id 역순으로 안정적으로 갈린다.

#### 이 선택에서 파생되는 두 가지

- **`-limit`이 없을 때(전체 출력)는 별도 경로가 필요하다.** `nlargest`에 `None`을 넘길 수 없고,
  애초에 전체를 출력하려면 전부 정렬해야 한다. 기본 limit 값을 두고 항상 `nlargest`를 타게 할지,
  limit 미지정 시에만 다른 경로로 갈지 **6단계에서 결정할 것**
- **스트리밍의 이점이 "실행 시간"이 아니라 "메모리"가 된다.** `nlargest`는 파일 끝까지 읽으므로
  I/O는 줄지 않는다. 따라서 시연(9번)도 시간 비교가 아니라 **메모리 관점**으로 보여줘야 한다

### 4. 데코레이터

| 용어 | 뜻 | 왜 알아야 하나 |
|---|---|---|
| **데코레이터 (decorator)** | 함수를 감싸서 **원본을 수정하지 않고** 동작을 추가하는 함수. `@name`으로 적용 | 명세 필수 요구사항. 1개 이상 구현하고 실제로 적용해야 함 |
| **횡단 관심사 (cross-cutting concern)** | 예외 처리·로깅·시간 측정처럼 **여러 기능에 공통으로 흩어지는 코드** | 10개 명령마다 같은 `try/except`를 쓰면 그게 바로 횡단 관심사. 데코레이터가 존재하는 이유 |
| **`functools.wraps`** | 감싼 함수가 원본의 이름·docstring을 유지하도록 해주는 데코레이터 | 안 붙이면 모든 함수 이름이 `wrapper`로 보여서 디버깅이 지옥이 된다. **거의 항상 붙인다** |
| **클로저 (closure)** | 바깥 함수의 변수를 기억하는 안쪽 함수 | 데코레이터의 작동 원리 그 자체 |
| **인자 있는 데코레이터** | `@retry(times=3)`처럼 옵션을 받는 데코레이터 (함수를 3겹으로 중첩) | 필수는 아니지만, 필요해지는 순간이 오면 구조를 알아야 한다 |

### 5. CLI 구성

| 용어 | 뜻 | 왜 알아야 하나 |
|---|---|---|
| **`argparse`** | 표준 라이브러리 CLI 인자 파서 | `-help` 자동 생성이 명세 요구사항을 바로 만족시킨다 |
| **서브커맨드 (`add_subparsers`)** | `python -m budget_app add` 처럼 하나의 진입점 아래 여러 명령을 두는 패턴 | 10개 명령을 구분하는 방식 |
| **중첩 서브커맨드** | 서브커맨드 아래 또 서브커맨드 (`category add`, `budget set`) | `category`는 `add/list/remove`, `budget`은 `set`을 가진다 — **2단계 중첩이 필요하다**. 처음 짤 때 놓치기 쉬움 |
| **부모 파서 (`parents=[...]`)** | 여러 서브커맨드가 공통 옵션을 공유하게 하는 방법 | `-data-dir`을 10개 명령에 각각 쓰지 않으려면 필요 |
| **`python -m <package>`** | 패키지를 모듈로 실행하는 방식 | 명세의 권장 실행 형태. 패키지 안에 **`__main__.py`가 있어야** 동작한다 |
| **대화형 입력 루프** | `input()`으로 받고, 검증 실패 시 다시 묻는 흐름 | `add`의 기본 방식. "재입력 요구 또는 오류 메시지"가 명세 요구사항 |

### 6. 예외 처리 / 종료 코드

| 용어 | 뜻 | 왜 알아야 하나 |
|---|---|---|
| **종료 코드 (exit code)** | 프로그램이 OS에 남기는 정수. 관례상 0=성공, 그 외=실패 | 명세 필수. 터미널에서 `echo $?`로 확인 가능 |
| **`sys.exit(n)`** | 종료 코드를 지정하며 종료 | `sys.exit(1)` 같은 형태. `SystemExit` 예외를 발생시키는 방식이라 `except Exception`에 안 잡힌다 |
| **커스텀 예외 클래스** | `class ValidationError(Exception)` 처럼 직접 정의한 예외 | "내가 의도한 오류"와 "진짜 버그"를 구분하는 장치. 전자만 사람이 읽을 메시지로 바꾸고, 후자는 숨기면 안 된다 |
| **스택트레이스 억제** | 예외를 잡아 사람이 읽을 메시지만 출력하는 것 | 명세가 금지하는 건 "**스택트레이스를 사용자에게 보여주는 것**". 원인 + 해결 힌트 형태로 바꿔 출력한다 |

### 7. 파이썬 함정 모음

| 함정 | 내용 |
|---|---|
| **`import`는 예약어** | `def import(...)` 는 문법 오류. 함수명은 `run_import` / `cmd_import` 등으로 |
| **가변 기본 인자** | `def f(items=[])`, `tags: list[str] = []` 둘 다 **인스턴스 간 공유** 버그. `None` 또는 `default_factory` |
| **`float`로 금액 다루기** | `0.1 + 0.2 != 0.3`. 명세가 "양수 정수"라고 못 박았으니 **`int`로 다룬다** |
| **`datetime.strptime`의 관대함** | `"2024-1-5"`도 `%Y-%m-%d`로 통과한다. 형식을 엄격히 볼 거면 별도 검사가 필요 |

---

## 진행 계획

Subject.txt의 기능 요구사항을 **학습 순서**로 재배열한 단계별 계획.
구현 세부는 일부러 적지 않았다 — 직접 설계하는 게 목적.

### 0단계 — 사전 준비
- **목표**: 저장소 초기화, `.gitignore` 정리(`.DS_Store` 추적 해제), 설계 결정 4가지를 문서로 고정
- **완료 조건**: `python -m budget_app`가 (아직 아무것도 못 해도) 에러 없이 실행됨
- **생각해볼 질문**: `data/` 디렉토리를 git에 커밋할 것인가, 말 것인가? 근거는?

### 1단계 — 모델 계층 (`models.py`)
- **목표**: `Transaction` dataclass 정의 (+ 필요하다면 `Budget`, `Category`)
- **학습 목표**: dataclass, 타입 힌트, `field(default_factory)`, dict ↔ dataclass 변환
- **완료 조건**: 파일 I/O 코드가 **한 줄도 없이** 모델만으로 인스턴스 생성·dict 변환이 됨
- **생각해볼 질문**: `id` 생성 책임은 모델에 있는가, 서비스에 있는가? 왜?

### 2단계 — 저장소 계층 (`storage.py`)
- **목표**: JSONL 읽기(제너레이터)/append 쓰기/원자적 전체 재작성, 최초 실행 시 파일 생성 + 카테고리 시드
- **학습 목표**: `yield`, `pathlib`, `os.replace()`, "파일 없을 때"와 "파일 비었을 때"의 구분
- **완료 조건**: 빈 임시 디렉토리를 가리켜도 파일 3개가 생기고, 카테고리 4개가 **최초 1회만** 시드됨
- **생각해볼 질문**: 저장소가 `Transaction` 타입을 알아야 하는가, 아니면 dict만 다뤄야 하는가? 어느 쪽이 계층 분리에 맞는가?

### 3단계 — CLI 골격 (`cli.py` + `__main__.py`)
- **목표**: 10개 서브커맨드의 **껍데기만** 만들고 `-help`가 전부 뜨게 하기. `-data-dir` 공통 옵션, 종료 코드 처리
- **학습 목표**: `add_subparsers()`, 중첩 서브커맨드(`category add`, `budget set`), `parents=`, `sys.exit()`
- **완료 조건**: 10개 명령 전부 `-help`가 나오고, 없는 명령은 0이 아닌 코드로 종료
- **참고**: 이 단계에선 **일부러 데코레이터를 쓰지 않는다.** 중복을 먼저 겪어야 11단계가 의미 있다
- **생각해볼 질문**: `category`와 `budget`의 2단계 중첩을 argparse에서 어떻게 표현하는가?

### 4단계 — `category add` / `category list`
- **목표**: 가장 단순한 도메인으로 **4계층 왕복을 처음 완주**하기
- **학습 목표**: CLI → 서비스 → 저장소 → 모델 → 다시 출력까지의 데이터 흐름
- **완료 조건**: 카테고리 추가/조회가 되고, 중복 추가 시 안내 메시지
- **생각해볼 질문**: 여기서 만든 호출 흐름이 나머지 9개 명령의 템플릿이 된다. 지금 어색한 부분이 있다면 지금 고쳐라

### 5단계 — `add` (거래 추가)
- **목표**: 대화형 입력 + 검증 + id 발급 + 저장
- **학습 목표**: `input()` 재입력 루프, 날짜/금액/타입/카테고리 검증, 검증 실패 메시지 설계
- **완료 조건**: 잘못된 날짜·음수 금액·미등록 카테고리를 각각 넣어봐도 죽지 않고 재입력을 요구함. 저장 후 `TX-000012` 형태의 id 출력
- **확정(⑥)**: 순번은 저장하지 않는다. `add` 때마다 `transactions.jsonl`을 스트리밍하며 **최대 순번 + 1**을 계산한다. 카운터 파일을 두지 않으므로 데이터와 카운터가 어긋날 여지가 없고, 저장 파일도 3개로 유지된다
- **⚠️ 알려진 한계**: 마지막 거래를 `delete`하면 **id가 재사용된다.** `TX-000012`를 지운 뒤 `add`하면 다시 `TX-000012`가 나온다. 예전에 export한 CSV와 의미가 어긋날 수 있으므로 **README에 명시할 것**
- **생각해볼 질문**: 이 스캔을 어느 계층에 둘 것인가? 저장소가 "다음 id"를 알아야 하는가, 서비스가 스캔 결과로 계산해야 하는가?

### 6단계 — `list` (목록 조회)
- **목표**: 제너레이터 스트리밍으로 최신순 + `-limit N`
- **학습 목표**: `yield`의 실제 효용, `itertools.islice`, **스트리밍과 정렬의 충돌**
- **완료 조건**: 거래를 수십 건 넣어두고 `-limit 3`이 정확히 최신 3건을 냄
- **확정(⑤)**: `heapq.nlargest(limit, stream, key=lambda tx: (tx.date, tx.id))`
- **생각해볼 질문**: `-limit`을 안 줬을 때는 어떤 경로로 처리할 것인가? (기본 limit 값을 두고 항상 nlargest / limit 미지정 시 전체 정렬)
- **생각해볼 질문**: 이 선택의 이점은 시간이 아니라 메모리다. README에 그 근거를 어떻게 설명할 것인가?

### 7단계 — `search` (검색)
- **목표**: `-from -to -category -type -q -tag` 조합 검색
- **학습 목표**: 여러 필터를 **제너레이터 파이프라인**으로 연결하기 (조건마다 `if`를 중첩하지 않고)
- **완료 조건**: 조건을 아무것도 안 주면 전체, 여러 개 주면 AND로 좁혀짐
- **생각해볼 질문**: 필터 함수들을 리스트에 모아 `all(f(tx) for f in filters)`로 처리하면 어떤 이점이 있는가?

### 8단계 — `update` / `delete` / `category remove`
- **목표**: 파일 전체 재작성 계열 3종. 원자적 교체 적용
- **학습 목표**: `dataclasses.replace()`, `tempfile` + `os.replace()`, "사용 중인 카테고리" 검사
- **완료 조건**: 없는 id를 주면 안내 메시지 + 0이 아닌 종료 코드. 사용 중인 카테고리 삭제가 막히거나 대체 카테고리를 요구함
- **생각해볼 질문**: `-memo ""`로 메모를 지우는 것과, `-memo`를 아예 안 주는 것을 코드에서 어떻게 구분하는가?

### 9단계 — `budget set` + `summary`
- **목표**: 월 예산 저장, 월별 집계(총수입/총지출/잔액 + 카테고리별 지출 TOP N), 예산 사용률·초과 경고
- **학습 목표**: 스트리밍하며 누적 집계하기, `collections.Counter` / `heapq.nlargest`, 0으로 나누기 방어
- **완료 조건**: 데이터 없는 달은 "데이터 없음"을 명확히 출력. 예산이 있으면 사용률(%)과 초과 경고가 함께 나옴
- **생각해볼 질문**: 예산 대비 사용률은 "지출"만 기준인가, "잔액" 기준인가? 어느 쪽이 사용자 기대에 맞는가?

### 10단계 — `import` / `export` (CSV 경계)
- **목표**: 고정 스키마 CSV로 일괄 등록/내보내기
- **학습 목표**: `csv.DictReader/DictWriter`, `newline=""`, 행 단위 오류 격리
- **완료 조건**: `imported=N, skipped=M` 형태 출력. **잘못된 행 하나가 전체를 죽이지 않음**. `export`는 `-month` 또는 `-from/-to` 중 하나를 **필수**로 요구
- **주의**: CSV 스키마에 **`id` 컬럼이 없다.** 따라서 `export` → `import` 왕복은 **새 id로 중복 등록**된다. 버그가 아니라 명세대로의 동작이므로 README에 명시할 것
- **생각해볼 질문**: `import`가 `add`와 **같은 검증 로직**을 타고 있는가? 아니라면 어디가 새고 있는가?

### 11단계 — 데코레이터 + 예외/종료 코드 정리
- **목표**: 10개 명령에 흩어진 `try/except`·로그를 데코레이터로 추출
- **학습 목표**: `functools.wraps`, 횡단 관심사 분리, 데코레이터를 **어느 계층에 걸 것인가**
- **완료 조건**: 어떤 오류 상황에서도 스택트레이스가 안 보이고, 원인 + 해결 힌트가 출력되며, 종료 코드가 0이 아님
- **확정(⑦)**: **CLI 진입점**에 건다. `cmd_add` 등 10개 핸들러에 `@handle_errors`를 붙이고, 서비스는 예외를 `raise`만 한다. 데코레이터가 예외를 `[오류] 원인` + `[힌트] 해결책` 형태로 번역하고 `sys.exit(1)`을 호출한다
- **핵심**: 이 구조라야 **서비스 계층이 `print`와 `sys.exit`을 끝까지 모른 채로** 남는다. 서비스에 걸면 계층이 샌다
- **생각해볼 질문**: 내가 정의한 예외(`ValidationError` 등)와 예상 못 한 버그(`KeyError` 등)를 데코레이터가 똑같이 취급해도 되는가? 후자까지 조용히 삼키면 어떤 문제가 생기는가?

### 12단계 — 문서화 + push
- **목표**: README.md 작성, GitHub push
- **완료 조건**: 실행 방법 / 저장 파일 위치·형식 / 주요 명령 예시 / **import·export CSV 스키마** / **설계 결정 4가지** 포함
- **생각해볼 질문**: 이 README만 보고 처음 보는 사람이 그대로 실행할 수 있는가?

---

## 시연 시뮬레이션 (구현 전 리허설)

코드를 짜기 전에 "완성되면 시연이 어떻게 흘러갈지"를 미리 그려둔다.
각 항목이 그대로 구현의 완료 기준이 된다. (보너스 시연은 제외)

### 사전 준비
- 빈 상태에서 시작 — `data/` 삭제 후 첫 실행이 어떻게 되는지가 ②번 결정의 증명이다
- 시연 중간중간 `echo $?`로 종료 코드를 보여줄 것 (명세 필수 요구사항의 유일한 가시적 증거)

### 1. 최초 실행 — 자동 초기화
```
$ rm -rf data && python -m budget_app category list
[안내] data/ 디렉토리와 저장 파일을 생성했습니다.
[안내] 기본 카테고리를 등록했습니다: food, transport, rent, etc
- food
- transport
- rent
- etc
```

### 2. 정상 흐름 — 거래 추가
```
$ python -m budget_app add
날짜(YYYY-MM-DD): 2024-01-15
타입(income/expense): expense
카테고리: food
금액(양수): 15000
메모(선택): 점심
태그(쉼표로 구분, 없으면 엔터): meal
[저장 완료] id=TX-000012
```

### 3. 예외 상황 — 입력 검증 (재입력 요구)
```
$ python -m budget_app add
날짜(YYYY-MM-DD): 2024-13-40
[오류] 날짜 형식이 올바르지 않습니다 (YYYY-MM-DD).
[힌트] 예: 2024-01-15
날짜(YYYY-MM-DD): 2024-01-15
타입(income/expense): 지출
[오류] 타입은 income 또는 expense만 가능합니다.
타입(income/expense): expense
카테고리: 커피
[오류] 등록되지 않은 카테고리입니다: 커피
[힌트] 'category add'로 먼저 등록하거나, 'category list'로 목록을 확인하세요.
카테고리: food
금액(양수): -500
[오류] 금액은 1 이상의 정수여야 합니다.
금액(양수): 15000
...
```

### 4. 목록 / 검색
```
$ python -m budget_app list -limit 3
TX-000012 | 2024-01-15 | expense | food      | 15000   | 점심
TX-000011 | 2024-01-14 | income  | salary    | 3000000 |
TX-000010 | 2024-01-12 | expense | transport | 20000   |

$ python -m budget_app search -from 2024-01-01 -to 2024-01-31 -category food
TX-000012 | 2024-01-15 | expense | food | 15000 | 점심
(1건)
```

### 5. 예산 + 요약
```
$ python -m budget_app budget set -month 2024-01 -amount 500000
[저장 완료] 2024-01 예산 500000원

$ python -m budget_app summary -month 2024-01 -top 3
총 수입: 3000000원
총 지출: 215000원
잔액: 2785000원
예산: 500000원 (사용률 43.0%)

지출 TOP 3
1) rent      150000원
2) food       45000원
3) transport  20000원
```

예산 초과 케이스도 반드시 같이 시연:
```
$ python -m budget_app summary -month 2024-02
...
예산: 100000원 (사용률 215.0%)
[경고] 예산을 115000원 초과했습니다.
```

데이터 없는 달:
```
$ python -m budget_app summary -month 2023-05
[안내] 2023-05에 해당하는 데이터가 없습니다.
```

### 6. 수정 / 삭제
```
$ python -m budget_app update -id TX-000012 -amount 18000
[수정 완료] TX-000012

$ python -m budget_app update -id TX-999999 -amount 1000
[오류] 해당 id의 거래를 찾을 수 없습니다: TX-999999
[힌트] 'list'로 존재하는 id를 확인하세요.
$ echo $?
1

$ python -m budget_app delete -id TX-000012
[삭제 완료] TX-000012
```

### 7. 카테고리 삭제 — 사용 중 처리
```
$ python -m budget_app category remove
카테고리명: food
[오류] 'food'를 사용 중인 거래가 3건 있습니다.
[힌트] 해당 거래를 먼저 수정하거나, 대체 카테고리를 지정하세요.
```

### 8. import / export
```
$ python -m budget_app export -out export.csv -month 2024-01
[완료] export.csv (12 records)

$ head -2 export.csv
date,type,category,amount,memo,tags
2024-01-15,expense,food,15000,점심,meal

$ python -m budget_app import -from import.csv
[완료] imported=5, skipped=1
[안내] 2행: 등록되지 않은 카테고리입니다 (커피) — 건너뜀
```

### 9. 스트리밍 증명 — **메모리** 관점으로

⑤번 결정(`heapq.nlargest`)의 이점은 **실행 시간이 아니라 메모리**다.
`nlargest`는 파일 끝까지 읽으므로 I/O는 줄지 않는다. 따라서 시간 비교로는 아무것도 증명되지 않는다.

증명 방법 (하나 골라서 미리 정해둘 것):
- 수십만 줄짜리 더미 `transactions.jsonl`을 만들고, `tracemalloc`(표준 라이브러리)으로
  **최대 메모리 사용량**을 `nlargest` 방식 vs `sorted(전체 리스트)` 방식으로 비교
- 또는 `/usr/bin/time -l`(macOS)의 maximum resident set size 비교
- 더미 파일 생성 스크립트도 표준 라이브러리만으로 짤 것 (제출물에 포함할지는 선택)

### 10. `-help`
```
$ python -m budget_app -help
$ python -m budget_app add -help
$ python -m budget_app category -help
```

---

## 최종 제출 체크리스트 (Subject.txt 4장 총정리)

### 실행 및 입력
- [ ] `python -m budget_app <command> [options]`로 실행됨
- [ ] 모든 명령이 `-help`로 사용법 출력
- [ ] `add`는 대화형 입력
- [ ] 모든 옵션이 `-` **단일 대시** 표기 (7장 제약사항)
- [ ] `-help`와 `--help` 둘 다 동작 (4장이 `--help`를 명시하므로 양쪽 등록)
- [ ] `-from` / `-to`에 `dest=` 지정 (`from`은 파이썬 예약어)
- [ ] `-data-dir`로 저장 폴더 변경 가능

### 데이터 모델
- [ ] `id / type / date / amount / category / memo / tags` 필드 존재
- [ ] dataclass(또는 준하는 구조)로 정의
- [ ] 클래스 **2개 이상** 사용
- [ ] 타입 힌트 적용

### 저장
- [ ] JSONL 포맷, 파일 **3개 이상** (`transactions` / `categories` / `budgets`)
- [ ] 파일 없을 때 자동 생성 + 기본 카테고리 시드 (**최초 1회만**)
- [ ] `update`/`delete`가 원자적 교체로 안전하게 동작

### 기능 10종
- [ ] `add` — 대화형, 카테고리 검증, 생성된 id 출력
- [ ] `list` — 최신순, `-limit`, **제너레이터 스트리밍** (`heapq.nlargest`, 키는 `(date, id)`)
- [ ] `search` — `-from -to -category -type -q -tag`, 최신순, 스트리밍
- [ ] `summary` — `-month`, `-top`, 총수입/총지출/잔액, 데이터 없는 달 처리
- [ ] `budget set` — `-month -amount`, summary에 사용률·초과 경고 반영
- [ ] `category` — `add` / `list` / `remove` (사용 중 처리 포함)
- [ ] `update` — **옵션 기반** `-id` + 부분 수정, 없는 id 처리
- [ ] `delete` — `-id`, 없는 id 처리
- [ ] `import` — `-from <csv>`, 행 단위 오류 격리, `imported/skipped` 출력
- [ ] `export` — `-out <csv>`, `-month` 또는 `-from/-to` **필수**

### 구조 / 품질
- [ ] 모듈 **3개 이상** (계층형 4모듈)
- [ ] 데코레이터 **1개 이상 구현하고 실제 적용** (CLI 진입점에 `@handle_errors`)
- [ ] 서비스 계층에 `print` / `sys.exit` 호출이 **한 군데도 없음**
- [ ] 스택트레이스 출력 없음, 원인 + 해결 힌트 형태
- [ ] 정상 종료 0 / 오류 종료 0 아님
- [ ] 표준 라이브러리만 사용

### 문서
- [ ] README에 실행 방법
- [ ] README에 저장 파일 위치·형식
- [ ] README에 주요 명령 예시
- [ ] README에 **import/export CSV 스키마**
- [ ] README에 **설계 결정 8가지** (특히 `update` 방식, 초기 카테고리 정책, 정렬 방식의 근거)
- [ ] README에 **id 재사용 한계** 명시 (마지막 거래 삭제 후 같은 id 재발급)
- [ ] README에 **export → import 왕복 시 새 id로 중복 등록**됨을 명시
