# B2-1 요구사항 체크리스트

기준: `Subject.txt` 원문. 정리일: 2026-09-11.

- `[x]`: 기존 코드 검토와 실행 검증에서 충족을 확인한 항목.
- `[ ]`: 미구현 또는 충족 여부 확인이 남은 항목.
- 기능별 완료 표시는 공통 오류 처리까지 완료됐다는 뜻은 아니다.
- 원문에 없는 설계 선택·개선 제안은 포함하지 않는다. 원문이 허용한 선택지는 그대로 표시한다.

### 1. 거래 데이터 모델 — 원문 4장

- [x] Transaction에 `id`, `type`, `date`, `amount`, `category`, 선택 필드 `memo`, `tags` 포함
- [x] 거래 ID의 유일성 확보
- [x] 타입은 `income` 또는 `expense`, 날짜는 `YYYY-MM-DD`, 거래 금액은 양수
- [x] 데이터 모델을 dataclass 또는 그에 준하는 구조로 정의

### 2. 저장과 초기 실행 — 원문 2·4·7장

- [x] JSONL 또는 CSV 중 하나를 선택해 영구 저장
- [x] 거래·카테고리·예산을 포함해 저장 파일을 3개 이상으로 분리
- [x] 옵션으로 저장 폴더 변경 가능
- [x] 저장 파일이 없으면 자동 생성하거나 초기화 안내 출력
- [x] 카테고리 파일이 비어 있으면 기본 카테고리 자동 생성 또는 category add 안내 후 add 차단

### 3. 실행과 입력 — 원문 4·7장

- [x] 모든 명령에서 `--help`로 사용 방법 출력
- [x] 입력 기본 방식은 대화형으로 제공 (`search`, `list`, `summary`, `export`, `import`, `delete`는 옵션 방식 허용; update는 8장 참고)

원문 표기 충돌: 4장은 “옵션 표기는 리눅스 표준인 `--`로 통일”, 7장은 “옵션 표기는 `-`로 통일”이라고 명시한다. 두 문구를 그대로 기록하며, 별칭 병행을 원문 필수 조건으로 추가하지 않는다.

### 4. 카테고리 추가·조회 — 원문 2·4장

- [x] `category add`로 카테고리 추가 및 결과 출력
- [x] `category list`로 카테고리 목록 출력

### 5. 거래 추가와 입력 검증 — 원문 2·4장

- [x] add 실행 후 날짜·타입·카테고리·금액·메모/태그를 대화형으로 입력받아 저장
- [x] 날짜 형식 오류를 재입력 요구 또는 오류 메시지로 처리
- [x] 음수·0 거래 금액을 재입력 요구 또는 오류 메시지로 처리
- [x] 허용되지 않은 타입을 재입력 요구 또는 오류 메시지로 처리
- [x] 미등록 카테고리를 안내하고 재입력 또는 등록 유도
- [x] 저장 성공 메시지와 생성된 거래 ID 출력

### 6. 거래 목록 — 원문 4장

- [x] list에서 최신순 거래 목록 출력
- [x] `--limit N` 옵션과 기본값 제공
- [x] 파일 전체를 한 번에 로드하지 않고 제너레이터 기반 스트리밍 처리

### 7. 거래 검색 — 원문 4장

- [x] 기간 검색: `--from`, `--to`
- [x] 카테고리 검색: `--category`
- [x] 타입 검색: `--type`
- [x] 메모 키워드 검색: `--q`
- [x] 태그 검색: `--tag`
- [x] 검색 결과를 최신순으로 출력
- [x] 제너레이터 기반 스트리밍 유지

### 8. 거래 수정·삭제 — 원문 2·4장

- [x] ID 기반 거래 수정 제공
- [x] update를 옵션 방식 또는 대화형 방식 중 하나로 구현
- [ ] 선택한 update 방식을 문서에 명확히 고정
- [x] `delete --id <id>`로 특정 거래 삭제
- [x] 존재하지 않는 ID를 “없는 데이터”로 처리하고 사용자 메시지 출력
- [x] 수정·삭제 성공/실패 메시지 출력
- [x] 파일 수정·삭제 시 저장 안정성 고려 (원문은 전체 재작성·임시 파일 등을 제시하고 원자적 교체를 권장)

### 9. 카테고리 삭제 — 원문 4장

- [x] `category remove` 제공 및 결과 출력
- [x] 사용 중인 카테고리는 삭제를 막거나 대체 카테고리 요구

### 10. 월별 요약과 예산 — 원문 2·4장

- [x] `summary --month YYYY-MM`으로 해당 월 요약 출력
- [x] 총수입·총지출·잔액(총수입−총지출) 출력
- [x] 카테고리별 지출 합계 TOP N 출력 및 top 옵션 제공
- [x] 데이터가 없는 달에 “데이터 없음” 명확히 출력
- [x] `budget set --month YYYY-MM --amount <금액>`으로 월 예산 저장
- [x] 예산 저장 성공 메시지 출력
- [x] 예산 데이터 영구 저장
- [x] 예산이 설정되면 summary에서 사용률(%)과 초과 여부 출력, 초과 시 경고

### 11. CSV 가져오기·내보내기 — 원문 2·4장

- [x] `import --from <csv>`로 거래 일괄 등록
- [x] `export --out <csv>`로 조건에 맞는 거래를 CSV로 저장
- [x] export에서 월 또는 시작일·종료일 범위 중 하나 이상을 필수로 요구
- [x] import/export 처리 건수 출력
- [x] UTF-8, 헤더 포함 CSV 사용
- [x] 아래 최소 스키마 적용

| 컬럼 | 필수 | 원문 설명 |
| --- | --- | --- |
| date | Y | YYYY-MM-DD |
| type | Y | income / expense |
| category | Y | 등록된 카테고리 |
| amount | Y | 양수 정수 |
| memo | N | 문자열 |
| tags | N | 쉼표(,) 구분 문자열 |

### 12. 데코레이터와 공통 오류 처리 — 원문 4·7장

- [x] 공통 관심사 데코레이터를 1개 이상 직접 구현하고 실제 적용 (시간 측정; main에 적용, 임시 폴더 CLI 실행으로 시간 출력과 기존 종료 코드 유지 확인)
- [ ] 오류를 스택트레이스 대신 원인 + 해결 힌트로 출력
- [ ] 정상 종료는 0, 오류 종료는 0이 아닌 값으로 처리

### 13. README — 원문 2·4장

- [ ] 실행 방법 작성
- [ ] 저장 파일 위치·형식 작성
- [ ] 주요 명령 예시 작성
- [ ] import/export CSV 스키마 작성

update 방식의 문서 명시는 8장에서 관리한다.

### 14. 구조와 개발 환경 — 원문 1·3·4·6·7장

- [x] 최소 2개 이상의 클래스 사용
- [x] 최소 3개 이상의 모듈로 분리
- [x] 함수와 데이터 구조에 타입 힌트 적용
- [x] 표준 라이브러리만 사용
- [x] Python 3.10 이상 환경에서 실행 (기존 실행 검증 환경: Python 3.14)

### 15. 설명할 수 있어야 하는 내용 — 원문 3장

- [ ] 파일 기반 영구 저장과 CRUD·검색·요약·입출력 구현 설명
- [ ] 클래스·모듈 구조와 각 계층의 책임 설명
- [ ] yield 기반 제너레이터로 스트리밍 처리하는 이유와 동작 방식 설명
- [ ] 데코레이터로 공통 관심사를 분리한 구조와 이유 설명
- [ ] 타입 힌트의 이점을 실제 코드 예로 설명

원문 5장의 백업·반복 내역·테이블 정렬·저장 원자성 강화는 선택 보너스이므로 필수 체크리스트에서 제외한다.
