# B2-1 구현 체크리스트

기준: `Subject.txt`. 점검일: 2026-09-10.

- `[x]`: 현재 코드와 이전 실행 검증에서 완료를 확인한 항목.
- `[ ]`: 미구현 또는 보완·최종 확인이 필요한 항목.
- 기능별 완료 표시는 공통 오류 처리까지 모두 완료됐다는 뜻은 아니다. 남은 공통 요구사항은 12장, 최종 실행 검증은 15장을 참고한다.
- 보너스 기능은 필수 범위에 포함하지 않는다. 원문 충돌과 선택한 정책은 14장에 기록한다. 정책 결정은 구현 완료를 뜻하지 않는다.
- 실행 검증은 임시 폴더·임시 데이터로 수행하고 기존 `data/`는 변경하지 않는다.

### 1. 거래 데이터 모델 — `models.py`

- [x] `Transaction`을 `dataclass`로 정의하기
- [x] 필수 필드 정의하기: `id`, `type`, `date`, `amount`, `category`
- [x] 선택 필드 정의하기: `memo`, `tags`
- [x] 각 필드의 타입 힌트와 선택 필드의 기본값 정하기
- [x] 금액은 CSV 스키마와 일치하도록 양수 정수로 다룰 준비하기

**완료 기준:** 직접 거래 객체 하나를 만들고 각 필드 값을 확인할 수 있다.

### 2. 저장소 기본 기능 — `storage.py`

- [x] `TransactionRepository` 등 저장소 클래스 정의하기
- [x] 저장 폴더를 외부에서 전달받도록 만들기
- [x] JSONL 형식으로 거래를 한 줄씩 읽는 제너레이터 구현하기
- [x] 거래 한 건을 파일에 추가하는 기능 구현하기
- [x] 카테고리와 예산의 읽기·쓰기 기능 구현하기
- [x] 저장 폴더·파일이 없을 때 자동 생성하거나 초기화 안내하기
- [x] **빈 카테고리 파일** 정책 완성: 안 B(등록 안내 + 거래 추가 차단) 적용
  - 빈 `category list`는 등록 안내, `add`는 입력 전에 차단하고 `category add` 안내 (실행 검증 완료).

**완료 기준:** 거래를 저장한 뒤 프로그램을 다시 실행해도 읽을 수 있다. 현재 만들어둔 빈 파일도 정상 처리한다.

### 3. 실행과 CLI 뼈대 — `__main__.py`, `cli.py`

- [x] `python -m budget_app`으로 실행되도록 연결하기
- [x] `argparse`로 명령을 구분하기
- [x] `category add/list/remove`, `budget set`의 중첩 명령 구성하기
- [x] 저장 폴더 변경 옵션 연결하기 (`-data-dir`은 명령 앞에 지정, 생략 시 ./data; 거래·카테고리·예산 적용 및 폴더 간 분리 검증)
- [x] 현재 구현된 명령에서 `--help`로 도움말 출력
- [x] `summary --help` 도움말 제공
- [x] `export --help` 도움말 제공
- [x] `import --help` 도움말 제공
- [x] 모든 일반 옵션에 `--` 대표 표기와 기존 `-` 별칭을 함께 등록하기 (25개 등록, 8개 명령 조합의 두 표기 파싱 결과 동일 확인; 업무 처리 전체 실행 검증은 별도)
- [x] 최상위·중첩 명령 모두 `--help`, `-help`, `-h` 지원하기 (15개 파서의 3개 표기, 총 45회 임시 폴더 실행에서 동일한 도움말·종료 코드 0 확인)
- [ ] 도움말과 오류 힌트의 명령 예시를 `--` 표기로 통일하기

**완료 기준:** 도움말이 출력되고, 지정한 저장 폴더를 사용한다.

### 4. 카테고리 추가·조회 — `services.py`, `cli.py`

- [x] 서비스 계층에서 카테고리 추가·조회 처리하기
- [x] CLI에서 입력받아 서비스에 전달하기
- [x] 결과를 CLI에서 출력하기
- [x] 중복 카테고리 입력 시 동작 정하기

**완료 기준:** 추가한 카테고리가 재실행 후에도 목록에 나타난다.

### 5. 거래 추가 — `add`

- [x] 날짜·타입·카테고리·금액·메모·태그를 대화형으로 입력받기
- [x] 날짜 형식과 실제 존재하는 날짜인지 검증하기
- [x] 타입이 `income` 또는 `expense`인지 검증하기
- [x] 금액이 양수 정수인지 검증하기
- [x] 등록된 카테고리인지 검증하기
- [x] 유일한 거래 ID 생성하기
- [x] 검증에 성공한 거래를 저장하고 ID 출력하기
- [x] 날짜·타입·금액 등 일반 검증 오류에 원인과 해결 힌트 출력하기
- [x] 미등록 카테고리 오류에서 `category add`로 등록하도록 구체적으로 안내하기

**완료 기준:** 정상 거래는 저장되고, 잘못된 입력은 저장되지 않는다.

### 6. 거래 목록 — `list`

- [x] 최신순으로 거래 출력하기
- [x] `limit` 옵션과 **기본값** 제공하기
- [x] 파일을 읽는 과정에서 제너레이터 사용하기
- [x] 전체 거래를 리스트로 로드하지 않도록 구현하기
- [x] 같은 날짜의 거래 간 정렬 기준 정하기

현재 구현: `heapq.nlargest`로 지정한 개수만 선택한다.

**완료 기준:** 날짜를 뒤섞어 입력해도 지정한 개수의 최신 거래가 나온다.

### 7. 거래 검색 — `search`

- [x] 기간, 카테고리, 타입, 메모 키워드, 태그 검색 구현하기
- [x] 여러 조건을 함께 적용할 수 있도록 만들기
- [x] 검색 결과를 최신순으로 출력하기
- [x] 제너레이터 기반 스트리밍 유지하기
- [x] **결과 전체의 최신순 정렬과 메모리 사용을 함께 만족할 방법** 정하기

**완료 기준:** 복합 조건에 맞는 거래만 나오며, 검색 결과를 모두 메모리에 쌓아 정렬하지 않는다.

### 8. 거래 수정·삭제 — `update`, `delete`

- [x] `update` 입력 방식을 옵션 방식으로 고정하기
- [x] ID로 거래를 찾아 지정한 필드만 수정하기
- [x] 수정값에도 거래 추가와 동일한 검증 적용하기
- [x] 선택 필드의 “미지정”과 “내용 삭제” 구분하기
- [x] ID로 거래 삭제하기
- [x] 없는 ID는 안내 메시지와 오류 종료 코드로 처리하기
- [x] 파일 재작성의 안정성 확보하기 — 임시 파일과 원자적 교체 권장

- [x] `update`에 수정 옵션이 하나도 없으면 원인·힌트와 오류 종료 코드로 처리하기 (설계 선택). 빈 메모·빈 태그는 유효한 수정값으로 인정한다.

**완료 기준:** 대상 거래만 바뀌고 다른 거래는 보존된다.

### 9. 카테고리 삭제 — `category remove`

- [x] 해당 카테고리를 사용하는 거래가 있는지 검사하기
- [x] 사용 중이면 삭제를 막거나 대체 카테고리를 요구하기
- [x] 사용하지 않는 카테고리 삭제하기

**완료 기준:** 거래가 존재하지 않는 카테고리를 참조하는 상태를 만들지 않는다.

### 10. 월 예산과 요약 — `budget set`, `summary`

- [x] 월별 예산 저장하기
- [x] `budget set -month ... -amount ...` 옵션과 서비스 연결
- [x] 같은 월 재설정 시 기존 금액 갱신, 다른 월 보존
- [x] 실제 연월과 0 이상의 정수 예산 검증
- [x] 0원 예산은 “사용률 계산 불가: 예산 0원”으로 표시하고, 지출이 있으면 초과 경고 출력 (설계 선택)
- [x] 지정한 월의 총수입·총지출·잔액 계산하기
- [x] 거래 건수와 카테고리별 지출 합계 계산하기 (수입은 카테고리 지출에서 제외)
- [x] 해당 월의 예산을 요약에 연결하기: 미설정 `None`, 0원 `0` 구분; 거래 없는 달도 예산 반환
- [x] 예산 사용률과 초과 금액 계산하기 (미설정, 미달·동일·초과, 0원 예산, 거래 없는 달 검증)
- [x] 카테고리별 지출 합계 TOP N 출력하기
- [x] `top` 옵션 지원하기
- [x] 예산이 있으면 사용률과 초과 경고 출력하기
- [x] 거래가 없는 달은 “데이터 없음” 출력하기

**완료 기준:** 직접 계산한 값과 집계가 일치하고, 재실행 후에도 예산이 유지된다.

### 11. CSV 가져오기·내보내기 — `import`, `export`

- [x] 내보내기: UTF-8, 헤더 포함 CSV 작성
- [x] 가져오기: UTF-8, 헤더 포함 CSV 읽기 (필수 헤더·열 개수 검사, 한 행씩 반환)
- [x] 내보내기 스키마 고정: `date,type,category,amount,memo,tags`
- [x] 가져오기 필수 헤더 date/type/category/amount 확인; memo/tags 생략과 열 순서 변경 허용
- [x] `tags`를 쉼표 구분 문자열로 변환하기
- [x] 가져오는 거래에 기존 검증 로직 적용하기
- [x] 내보내기 처리 건수 출력하기
- [x] 가져오기 처리 건수 출력하기
- [x] CLI import -from 연결; 파일 없음·CSV 헤더/열 개수 오류 안내와 오류 종료 확인
- [x] 내보내기 서비스에서 월 또는 시작일·종료일 조건 검증 (잘못된 조건은 파일 생성 전 거부)
- [x] CLI에 export 명령과 출력 경로·월·날짜 범위 옵션 연결
- [x] 조건에 맞는 거래를 CSV로 저장하기 (월·기간·교집합·경계일·빈 결과와 건수 검증)

#### 남은 CSV 보완

- [x] 중복 헤더를 구조 오류로 거부하기 (설계 선택; 임시 CSV로 행 저장 전 거부·원인과 힌트·종료 코드 1 확인)
- [ ] 건너뛴 행의 위치와 원인·해결 힌트를 CLI에서 안내하기
- [ ] `skipped > 0`이면 종료 코드 `1`을 반환하기 (현재는 전체 순회 완료 시 `0`)
- [ ] 구조 오류나 파일 오류로 중단되면 이미 저장된 정상 행은 유지된다는 안내 제공하기
- [ ] export 쓰기 실패 시 부분 CSV가 남을 수 있음을 안내하기. 부분 파일 자동 정리는 선택 개선으로 둔다.

**완료 기준:** 한글, 쉼표가 있는 메모, 여러 태그가 올바르게 읽히고 저장된다. 실패한 행과 중단 여부를 알 수 있고, 종료 코드와 부분 저장 정책이 일치한다.

### 12. 공통 오류 처리와 데코레이터

- [ ] 공통 관심사 데코레이터를 직접 구현하고 CLI 실행 경계에 실제 적용하기
- [ ] 저장소 생성·대화형 입력·제너레이터 순회 중 발생하는 오류도 공통 처리 범위에 포함하기
- [ ] 오류 출력에 원인과 해결 힌트를 포함하고 스택트레이스가 노출되지 않도록 처리하기
- [ ] `EOFError`는 입력 종료 안내와 종료 코드 `1`로 처리하기
- [ ] `KeyboardInterrupt`는 취소 안내와 종료 코드 `130`으로 처리하기 (설계 선택)
- [ ] 저장소 생성부터 읽기·쓰기·교체까지 발생하는 `OSError`를 처리하기
- [ ] argparse의 필수 옵션 누락·정수 변환 실패 등에 해결 힌트를 제공하고 종료 코드 `2` 유지하기
- [ ] 검색 기간 역전 오류의 힌트에 시작일·종료일 순서 확인을 포함하기
- [x] 현재 정상 명령 `0`, 일반 검증 오류 `1`, 파서 오류 `2` 확인 (import 행 건너뛰기 정책 변경은 11장)

### 13. 사용자 문서와 공부 노트 정리

- [ ] README에 실행 방법과 Python 버전 작성하기
- [ ] 저장 파일 위치·JSONL 형식과 `--data-dir`의 기본값·지정 위치 설명하기
- [ ] 주요 명령 예시를 `--` 표기로 작성하고 단일 대시 별칭 지원 안내하기
- [ ] import/export CSV 스키마와 헤더 허용 범위 설명하기
- [ ] import의 행 건너뛰기·구조 오류 중단·부분 저장·종료 코드 정책 설명하기
- [ ] import 재실행 시 중복 등록될 수 있으며 실패한 행만 고쳐 재시도하도록 안내하기
- [ ] 옵션 기반 update, 미지정과 빈 값의 차이, 수정 옵션 없는 요청의 처리 설명하기
- [ ] 빈 카테고리 정책과 category add 선행 안내 작성하기
- [ ] 검색·정렬·ID·예산·export 정책과 한계를 14장에 맞춰 설명하기
- [ ] Study.md의 단일 대시 우선 단정, 도움말 표기, 기본 카테고리 생성, 검색 방식, import 종료 정책을 현재 방침과 일치시키기
- [ ] Study.md에서 설계 선택을 원문 필수 조건으로 표현한 부분 정리하기
- [ ] 오래되거나 부정확한 코드 주석 정리하기: 검색 메모리 사용, 원자적 교체, `add_parser` 변수 등

### 14. 적용할 정책과 작업 범위

아래는 원문이 선택을 허용하거나 세부 동작을 명시하지 않은 부분의 설계 방침이다. 구현·문서화·실행 검증은 각 장에서 별도로 완료 표시한다.

| 항목 | 방침 |
| --- | --- |
| 옵션 표기 충돌 | 4장은 `--`, 7장은 `-`를 요구한다. `--`를 대표 표기로 쓰고 `-`를 호환 별칭으로 지원한다. 어느 장이 무조건 우선한다고 단정하지 않는다. |
| 도움말 | 모든 명령에 `--help`, `-help`, `-h`를 지원한다. 원문이 직접 명시한 것은 `--help`다. |
| 저장 폴더 | 생략 시 현재 작업 폴더 기준 `./data`. 저장 폴더 옵션은 명령 앞에 지정한다. |
| 빈 카테고리 | 안 B 유지: 등록을 안내하고 거래 add 입력 전에 차단한다. |
| 검색 | 조건은 AND, 날짜 양 끝 포함, 메모는 대소문자를 구분하는 부분 문자열, 태그는 항목 일치로 검색한다. |
| 정렬 | list/search는 거래 날짜 내림차순, 같은 날짜는 ID 숫자 내림차순이다. |
| 검색 메모리 | 100건씩 선택해 전체 결과를 반환한다. 파일 반복 스캔 비용이 있으며 검색 중 파일 불변·ID 유일성을 전제로 한다. |
| ID | 기존 ID 숫자 최댓값+1을 유지한다. 최대 ID 삭제 후 재사용될 수 있으며 동시 실행은 지원 범위에 포함하지 않는다. 원문은 영구 재사용 금지를 명시하지 않는다. |
| update | 옵션 기반. 미지정은 유지, 빈 메모·태그는 삭제, 수정 옵션이 없으면 오류로 처리한다. |
| 예산 | 0 이상의 정수. 0원은 사용률 계산 불가로 표시하고 지출이 있으면 초과 경고한다. 거래 없는 달도 설정된 예산을 표시한다. 별도 budget get은 추가하지 않는다. |
| CSV 헤더 | export는 6개 컬럼의 순서를 고정한다. import는 필수 4개를 요구하고 memo/tags 생략·순서 변경·추가 컬럼을 허용한다. 추가 컬럼은 저장하지 않으며 중복 헤더는 거부한다. |
| CSV 행 | 헤더에 없는 선택 컬럼은 빈 값으로 처리한다. 헤더가 있는 컬럼은 행에도 칸이 있어야 하며, 열 개수 불일치는 구조 오류로 중단한다. |
| import 저장 | 정상 행은 즉시 추가, 값 검증 오류 행은 건너뛴다. 구조·파일 오류는 중단하고 앞서 저장한 정상 행은 롤백하지 않는다. |
| import 종료 코드 | 전부 정상 또는 유효한 헤더만 있는 빈 데이터는 0. 건너뛴 행이 있거나 처리가 중단되면 1. 비영 종료가 롤백을 뜻하지 않는다. |
| CSV 재등록 | CSV에 ID를 내보내지 않는다. 같은 CSV를 다시 import하면 기존 거래와 중복 등록될 수 있다. |
| export 조건·순서 | 월 또는 완전한 날짜 범위 필수. 둘 다 지정하면 교집합. 날짜 범위는 항상 양쪽을 함께 요구한다. 출력은 현재 저장 순서를 유지한다. |
| export 파일 | 기존 파일 덮어쓰기 거부. 쓰기 실패 시 부분 CSV가 남을 수 있다. |
| 오류 처리 구조 | 전용 ValidationError 클래스는 도입하지 않는다. 서비스는 검증·업무 처리, CLI는 오류 안내·종료 코드를 담당한다. |

저장 파일의 JSON 손상·필드 누락·잘못된 구조에 대한 전용 진단과 복구는 현재 작업 범위에서 제외한다. 정상 저장 파일을 전제로 하며 파일 접근·쓰기 오류 처리는 12장에서 수행한다. 보너스 기능과 부분 export 파일 자동 정리는 필수 할 일에 포함하지 않는다.

### 15. 최종 시연·회귀 검증

- [x] 현재 구현의 최소 3개 모듈, 2개 클래스, 3개 저장 파일 조건 확인
- [x] 현재 타입 힌트·계층별 책임·표준 라이브러리 사용 확인
- [x] Python 3.14 환경에서 기존 기능 실행 확인
- [ ] 최종 제출 버전의 실행 환경·의존성과 Python 3.10 호환성 점검하기. 실제 실행한 버전과 코드 검토 범위를 구분해 기록한다.
- [ ] 모든 명령의 도움말과 일반 옵션의 두 표기가 동일하게 동작하는지 확인하기
- [ ] 임시 저장 폴더에서 초기 상태 → 카테고리 등록 → add/list/search/update/delete → budget/summary → export/import 전체 시연하기
- [x] 수정 옵션 없는 update 거부와 빈 메모·태그 삭제를 확인하기 (2026-09-11 임시 데이터 실행: 종료 코드·파일 보존·부분 수정 확인, 금액 0은 기존 검증에서 거부)
- [x] CSV 선택 컬럼 생략·순서 변경·추가 컬럼 무시·중복 헤더 거부를 확인하기 (2026-09-11 임시 데이터 CLI 실행, 정상 6개 헤더 포함 4가지 사례 검증)
- [ ] import 정상·부분 성공·전부 건너뜀·헤더만 있음·구조 오류 중단을 검증하고 건수·안내·종료 코드·실제 저장 결과 확인하기
- [ ] export 기존 파일 보호와 쓰기 실패 시 안내·부분 파일 상태 확인하기
- [ ] EOF·취소·argparse 오류·파일 접근/쓰기 오류의 원인·힌트·종료 코드 확인하기
- [ ] 파일 교체 실패 시 원본 보존·임시 파일 정리를 최종 변경 범위에 맞춰 확인하기
- [ ] README 예시와 실제 동작을 대조하고 CHECK 완료 상태 갱신하기

### 기존 실행 검증 기록

임시 데이터로 add, list, search(250건·복합 조건), update, delete, category 추가·조회·삭제 차단, budget 추가·갱신, summary, CSV import/export를 검증했다. 월·날짜 범위·교집합·경계일·빈 결과, 0원 예산, 저장 폴더 분리와 빈 카테고리 안내도 확인했다. 파일 재작성 실패 시 원본 보존·임시 파일 정리를 검증했으며 기존 `data/`는 변경하지 않았다.

이 기록은 당시 구현에 대한 검증이다. 일반 옵션 별칭은 구현하고 파서 실행으로 두 표기의 결과가 같음을 확인했다. 도움말 별칭은 임시 폴더에서 45회 실행 검증했다. 수정 옵션 없는 update 거부와 빈 메모·태그 삭제도 임시 데이터로 실행 검증했다. CSV 중복 헤더 거부와 기존 헤더 허용 정책도 임시 데이터 CLI 실행으로 검증했다. import 행 건너뛰기의 종료 코드 변경과 나머지 오류 안내 보완은 아직 구현·실행 검증하지 않았다.
